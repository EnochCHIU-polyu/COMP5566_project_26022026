import { AppFrame } from "../components/AppFrame";

export function EndPage() {
  return (
    <AppFrame>
      <section className="aw-card">
        <h1 className="aw-title text-2xl font-bold tracking-tight">End</h1>
        <p className="aw-subtle mt-2 text-sm">
          End page placeholder for future flow completion, summary, or sign-off
          actions.
        </p>
      </section>
    </AppFrame>
  );
}
